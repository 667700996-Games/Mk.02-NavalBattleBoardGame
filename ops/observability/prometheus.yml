global:
  scrape_interval: 30s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/mk01-rules/*.json

scrape_configs:
  - job_name: mk01
    metrics_path: /api/metrics
    scheme: http
    static_configs:
      - targets:
          - server:8080
        labels:
          environment: local
