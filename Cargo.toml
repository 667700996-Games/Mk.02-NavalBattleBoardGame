[workspace]
members = ["apps/server"]
exclude = ["fuzz"]
resolver = "2"

[workspace.package]
edition = "2024"
license = "MIT"
rust-version = "1.88"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
